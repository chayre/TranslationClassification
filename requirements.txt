# CDI Semantic Similarity — dependencies
# Install with:  pip install -r requirements.txt

sentence-transformers>=2.7.0   # LaBSE model + HuggingFace integration
torch>=2.0.0                   # PyTorch backend (CPU build is fine)
numpy>=1.24.0
openpyxl>=3.1.0                # Excel file creation (used by top10.py)